<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Anime Info</title>
    <link rel="stylesheet" href="/styles/main.css" />
  </head>
  <body>
    <h1>Anime Info Lookup</h1>
    <form action="/name" method="POST">
      <label for="anime-name">Enter Anime Name:</label>
      <input type="text" id="anime-name" name="animeName" required />
      <button type="submit">Search</button>
    </form>

    <% if (locals.anime) { %>
    <div class="anime-info">
      <h2>Anime Details</h2>
      <p><strong>Name:</strong> <%= anime.name %></p>
      <p><strong>ID:</strong> <%= anime.id %></p>
      <p><strong>Episodes:</strong> <%= anime.episodes || "N/A" %></p>
      <p><strong>Summary:</strong> <%= anime.summary || "N/A" %></p>
    </div>
    <% } %> <% if (locals.error) { %>
    <p style="color: red"><%= error %></p>
    <% } %>
  </body>
</html>
